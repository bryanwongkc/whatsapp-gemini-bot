
name: Keep Render Alive

on:
  schedule:
    - cron: "0,14,28,42,56 21,22,23,0-14 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render health endpoint
        run: curl -fsS https://whatsapp-gemini-bot-hhnl.onrender.com/healthz
